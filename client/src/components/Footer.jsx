import React from "react";

const footer = () => {
  return (
    <Box
      sx={{
        display: "flex",
        flexDirection: "row",
      }}
    >
      <Typography>Hello</Typography>
    </Box>
  );
};

export default footer;
